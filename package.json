{
  "name": "office-equipment-root",
  "version": "1.0.0",
  "description": "Root configuration for Office Equipment Management System",
  "scripts": {
    "postinstall": "npm install --prefix backend",
    "build": "npm run postinstall",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
